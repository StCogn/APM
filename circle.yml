machine:
  node:
    version: 6.10.0
test:
  pre:
    # - npm run lint
  override:
    - npm run test-once
# deployment:
  # production:
  #   branch: master
  #   heroku:
  #     appname: mock-smartlabs
